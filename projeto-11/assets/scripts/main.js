/**
 * //Algoritmo de soma
 * // var / let podem ter seu valor alterados normalmente
 * //algoritmo "semnome"
 * //var
 * //N1, N2, S: Inteiro
 let numero1 = 14.4;
 let numero2 = 1000.2345621;
 * //Constante não pode ter seu valor alterado
 * //S <- N1 + N2;
 const resposta = numero1 + numero2;
 * //Escreva("A soma é", s);
 * //fimalgoritmo;
 console.log("A soma é???: ", resposta);
 */

/**
  *  // Algoritmo de subtração
  let numero1 = 11;
  let numero2 = 53.32939812;
  
  const resposta = numero1 - numero2;
  
  console.log("a resposta é", resposta);
  */

/**
   * algoritmo "semnome"
    var
    N1, N2, S: Inteiro
    inicio
    Escreva ("Informe um Numero: ")
    Leia (N1)
    Escreva ("Informe outro Numero: ")
    Leia (N2)
   */
let name = prompt("digite seu nome: ");
let numero1 = prompt("Digite seu salarío bruto: ");

/**
 * S <- N1 + N2
 */
const resposta = numero1 - 100;

/**
   * Escreva ("A soma é", s)
fimalgoritmo
   */
console.log("seu salarío liquido é", resposta);
